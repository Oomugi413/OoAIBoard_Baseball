# Known Problems

このファイルは、コードレビューと動作確認で見つかった問題点を、
「直すべき」「直さなくていい」に分類して管理する。
対処が完了した項目は `3. 解決済みの記録` へ移す。

点検履歴:

- 1回目: 第2期手順1〜12完了後（コミット `778c329`、2026-07-02）。
- 2回目: 1回目の指摘6件の一括修正（コミット `88ff519`、2026-07-02）。
- 3回目: 2026-07-03。設計ファイルとコード全体の突き合わせ＋実ブラウザ確認。作業ツリー上の未コミット変更（実装希望リストのファウル・カウントRS・四球・死球・スマホ対応など）も点検対象に含む。
- 4回目: 2026-07-04。スコア入力画面をPC幅1366px・スマホ幅390pxで実画面確認。ボタン折り返し、スコアボード上部2段、カウント表示、英字略称フォントを重点確認。
- 5回目: 2026-07-04。ボタン折り返し・打者投手枠・カウント間隔・略称中央揃えの動作確認に加えて、対象を全画面・全ルートへ広げて探索。
- 6回目: 2026-07-13。アメフト版差分の取り込み（実装リスト20〜24）完了後の全体点検。設計ファイル一式とコード全体の突き合わせ、冗長コード・野球ルールとの矛盾の洗い出し、実ブラウザ確認（PC幅・390px幅、検証用ボードによるAPI実測。検証用データは点検後に削除済み）。指摘の一括修正（Codex）を反映済みで、修正されなかった残件だけを `1. 直すべき問題` に記載。なお、この回の検証ブラウザ環境はタブが非表示扱いになりrequestAnimationFrameが停止するため、GSAP演出の見た目は対象外とし、DOM状態・数値で確認した（演出自体は実装リスト17・24時点の実画面テストで確認済み）。
- 7回目: 2026-07-17。6回目の残件1.1〜1.4を修正し、単体テスト・ビルド・隔離保存先での実API/SSE確認を実施。

点検方法: 設計ファイルと実装の突き合わせ、コード通読、Codexによる指摘のレビュー、実ブラウザでの動作確認。

## 1. 直すべき問題

現在なし。

## 2. 直さなくていい問題

理由と、再検討する条件を記す。

### 2.1 プリセット保存が「プリセット単位の全項目・後勝ち」であること

- 指摘: Settings Pageの `savePreset` は全項目をまとめて送るため、保存直前に他端末が同じプリセットを更新していると後勝ちで上書きされる。
- 判断: 直さない。`operation.md` の「複数端末で編集した場合、最後に保存した内容を有効とする（後勝ち）」という仕様どおりの動作。競合が起きやすい編集メニュー（`board:patchConfig`）と選手名メニュー（`players:patch`）は既に差分パッチ化済みで、残るプリセット編集は単独の管理者が設定画面で行う前提のため、衝突の現実性が低い。
- 再検討する条件: 複数人が同時にプリセットを管理する運用が実際に始まったら、`presets:patch`（フィールド単位の差分保存）を追加する。

### 2.2 `players:patch` が変更ゼロでも `changed: true` を返す

- 指摘: 空のパッチでも `updatedAt` が更新され、SSEが1回配信される。
- 判断: 直さない。実害は再描画1回で、体感差はない。他の修正でこの周辺を触る機会があれば、`board:patchConfig` と同様の変更検知を入れる程度でよい。

### 2.3 SSEにキープアライブ（定期ping）がない

- 判断: 直さない。LAN内・数端末の直結構成では中間プロキシによる切断が起きず、切れてもEventSourceが自動再接続して `connected` イベントで全量同期する。
- 再検討する条件: リバースプロキシ越しの配信や、接続が頻繁に切れる症状が出たら。

### 2.4 Viewerのドラッグ中、pointermoveごとにlocalStorageへ書き込む

- 判断: 直さない。実測で動作は正常。書き込みは同期だがデータは小さく、対象端末（配信PC・スマホ）で体感の引っかかりは確認されていない。
- 再検討する条件: 低スペック端末でドラッグが目に見えてカクつく報告が出たら、ドラッグ終了時のみ保存に変える。

### 2.5 設定PATCHで送られた未知のキーがそのまま保存される

- 指摘: サーバーの `/api/settings` が `...body` で全キーを取り込むため、想定外のキーがapp.jsonに残り得る。
- 判断: 直さない。単独利用のローカルアプリで攻撃面がなく、既知キーは個別に正規化済み。気になる場合は許可キーだけ抽出する3行の修正だが、優先度は最も低い。

### 2.6 スコア入力の各ボタンがサーバー応答まで全体ロックされる

- 指摘: 操作のたびに `pending` で全ボタンが無効化されるため、超高速の連打はできない。
- 判断: 直さない。LANの応答は数ms〜数十msで体感できず、二重送信防止のメリットの方が大きい。

### 2.7 盗塁死用の `keepCurrentBatterOnNextInning` フラグが書き込まれるだけで読まれていない

- 指摘: `scoringRules.mjs` で盗塁死時にtrue、チェンジ時にfalseを設定するが、参照箇所がない（打者はチェンジで進まない仕様のため、フラグなしでも「盗塁死でも現在の打者を維持」は自然に満たされる）。
- 判断: 急いで直さない。実害はなく、`rules.md` の Open Questions で管理済み。次にこの周辺を触るときに削除する。

### 2.8 ページ切り替え時に一瞬白画面になることがある

- 指摘: コード分割（React.lazy）導入により、初めて開くページはチャンク取得までの間 `Suspense fallback={null}` で空白になる。
- 判断: 直さない。LANではチャンク取得が数ms〜数十msで知覚困難。気になる場合はfallbackにローディング表示を入れる程度でよい。

### 2.9 一時演出・攻守交代の中間表示の期限判定が端末の時計に依存する

- 指摘: HOME RUN/Kや中間表示の `expiresAt` はサーバー時刻で作られ、各端末は自分の `Date.now()` と比較する。端末の時計が大きくズレていると、演出が表示されない・長く残ることがあり得る（アメフト版はクロック用に時刻オフセット補正を持つが、野球版にはない）。
- 判断: 直さない。LAN内の端末は通常NTPで同期しており、ズレても演出の表示時間が変わるだけで試合データには影響しない。
- 再検討する条件: 実運用で「演出が出ない/消えない」端末が確認されたら、アメフト版のサーバー基準時刻方式（オフセット補正）を移植する。

### 2.10 折りたたみアニメーション中に対戦選手表示を素早く切り替えると一瞬ジャンプし得る

- 指摘: `useScoreboardFrame` はトゥイーン開始時に `topRef` を目標値へ即更新するため、0.5秒のアニメーション中に再度切り替えると、次のトゥイーンが「前回の目標値」から始まり、見た目が一瞬飛ぶ。
- 判断: 直さない。0.5秒以内に編集メニューで対戦選手表示を2回切り替える操作は現実的でなく、終了後は正しい状態に収束する。
- 再検討する条件: 実運用でチラつきが確認されたら、`onUpdate` で現在値を `topRef` に反映してから次のトゥイーンを開始する。

## 3. 解決済みの記録

### 2026-07-17（追加点検）で解決したもの

- 上限・下限やリセット済み状態で値が変わらない操作もundo履歴へ積まれていた問題を修正した。履歴対象のgame state・player settings・display optionsを操作前後で比較し、全項目が同じ場合だけ`changed: false`を返す。
- 野球版固有の成績記録を確認し、表示カウントが上限でも投手球数が増える投球や、打者成績・打順が変わる打席結果は履歴へ残し、「戻る」で成績を含めて復元する。
- サーバーは`changed: false`の操作では保存・SSE配信・last accessed at更新を行わない。
- 確認: 得点・ABS・カウント・アウトの実質変更なし操作、上限時の投手球数加算とUndo、打者安打・打順のUndoを単体テストで確認した。

### 2026-07-17（7回目の点検）で解決したもの

- 1.1 公開用のボード表現を`publicBoard()`へ集約し、ボード一覧・個別取得・作成・操作応答・SSEのpayload/stateからundo/redo履歴を除外した。履歴はサーバー内部と`app.json`には従来どおり保持する。操作後の明示的な`refresh()`を全画面から削除し、SSE更新へ一本化した。
- 1.2 Home Pageへ共通のTopBarを追加し、横3本線メニューからHOME・見る・動かす・設定へ移動できるようにした。
- 1.3 `operation.md`の試合終了条件とSSEイベント一覧、`data_model.md`のプリセット略称圧縮と履歴公開範囲、`scoreboard_design.md`の編集メニュー項目、`rules.md`の空の実装予定リスト表記を現行実装へ合わせた。
- 1.4 未使用だった`team:update`・`display:update`・`players:update`、`useScoreboardTimeline.js`、animationsの集約index、2つの色トークン、`/legacy`開発プロキシを削除し、OverlayEffectの古いviewBoxコメントを修正した。
- 確認: `npm test`成功、`npx vite build`成功、`node --check src/server/index.mjs`成功、`git diff --check`で空白エラーなし。隔離保存先の実API/SSE確認で、一覧・個別・作成・操作応答・SSEに履歴が含まれず、保存JSONにはundo履歴が残ることを確認した。

### 2026-07-13（6回目の点検）で解決したもの

- 1.1 履歴が選手名・表示設定も巻き戻す動作はユーザー判断で維持し、`operation.md`を現行実装へ合わせた。メニュー保存自体は履歴を増やさないが、既存スナップショットの復元時にはplayer settingsとdisplay optionsも戻る仕様を明記した。
- 1.2 `saveState()`をPromiseキューで直列化し、呼び出し時点のJSONを連番付き一時ファイルへ順番に保存するよう変更した。全API後の無条件保存を廃止し、状態変更時だけ保存する。隔離保存先へ40件の同時操作を送り、メモリ上・保存JSONとも最新得点40を保持することを確認した。
- 1.3 全体設定をフィールド単位のdirty管理へ変更した。SSEでは未編集フィールドだけを更新し、保存時は編集したフィールドだけをPATCHする。保存中に再編集した値もdirtyのまま保持する。
- 1.5 `Readme.md`のViewer操作、選手名メニュー、自動削除の説明を現行仕様へ修正した。
- 1.6 Viewer設定、履歴、試合終了、三振演出、実装完了番号に残っていた古い設計記述を削除・修正した。
- 1.4 ピッチャーへ永続IDを追加し、選手名メニューの編集・削除をID指定へ変更した。別端末が後から追加したピッチャーを削除対象に含めない。旧保存データと履歴スナップショットには起動時にIDを補完する。
- 1.7 対戦選手表示の保存先を`displayOptions.showMatchup`へ一本化した。現在打者・現在投手・守備側・打者成績・対戦要約を`src/shared/scoringRules.mjs`へ集約し、ロゴ変換とアップロードを`src/client/utils/teamLogo.js`へ共通化した。設計済みだった`src/shared/types.js`も追加した。
- 確認: `npm test`成功、`npx vite build`成功、`node --check src/server/index.mjs`成功、`git diff --check`で空白エラーなし。

### 2026-07-13（6回目の点検）で動作を確認したもの

- アメフト版差分の取り込み（実装リスト20〜24）が設計どおりであることを確認:
  - ボード単位の自動削除: `cleanupExpiredBoards` が各ボードの `lastAccessedAt` 基準で期限切れだけを削除し、`boards cleaned up` に削除ボードidを同梱。`lastAppAccessAt` は廃止済みで、旧保存データからも読み込み時に除去される。
  - プリセット削除時のリンク解除: 削除IDを持つ全ボードの先攻・後攻から `linkedPresetId` だけを解除し、対象ボードidをSSEで通知。読み込み済みのチーム設定値は変更しない。
  - Viewer: 選択枠が最大外形（1200/560）固定で盤面は右下揃い（対戦選手表示OFF時も枠は不変）、「非表示」トグルのON/OFFと `hiddenBoardIds` の保存・復帰、拡大率の下書き入力が「反映」まで表示サイズに影響しないこと（520px→反映後780px）、盤面外クリックでの選択解除を実測で確認。
  - TopBar: Homeを含む全6画面に横3本線アイコンとサイドメニューがあり、各主要画面へ遷移できる。
- 野球ルールとの突き合わせ: ファウル（2ストライク以降は据え置き）、四球・死球の押し出し（満塁で1点・満塁維持、一・三塁では三塁走者は動かない）、ホームラン（ランナー数+1を加点しランナー解消）、チェンジのアウト3必須と各リセット、投球数・投手三振数の加算先（守備側の現在投手）、試合終了の自動勝敗判定（同点は配色変更なし）に矛盾なし。
- 390px幅で全6ルートに横スクロールなし。コンソールエラーなし。`npm test`・`npx vite build` 成功。
- 検証用ボード・表示設定は点検後に削除・復元済みで、既存の3ボードには変更なし。

- 設定画面の全体設定フォームが、SSE更新（他画面のスコア操作など）のたびに入力中の値を失う問題 → dirty管理で修正済みを確認。
- 編集メニューの「略称拡大率(%)」数値入力が、入力のたびに即クランプされて実質入力不能だった問題 → blur/Enter時クランプへ修正済みを確認。
- 設定画面のプリセット「ロゴ削除」が選択済み未保存画像（pendingLogo）を消さない問題 → `clearPresetLogo` で両方クリアするよう修正済みを確認。
- 選手名メニューが開いた時点のスナップショットを丸ごと保存し、未編集の球数・成績まで巻き戻す問題 → 編集した項目だけを送る `players:patch` へ改善済みを確認。
- 一時演出がSSE再描画に依存し、期限切れ後も残ることがある問題 → クライアント側タイマーで期限時に消えるよう修正済みを確認。
- スコアリセット機能は `operation.md`（確認ダイアログ必須・リセット対象・履歴クリア）に文書化済みで、実装と一致することを確認。

### 2026-07-02（2回目、1.1〜1.6の一括修正）

- 1.1 チャンク分割: `src/client/main.jsx` を `React.lazy()` + `<Suspense>` に変更。ビルド結果は単一603.30kBから、最大チャンク328kB（`index-*.js`）＋ページ別チャンク（例: `ScoreInputPage` 40.89kB、`SettingsPage` 9.86kB）に分割され、500kB超の警告が解消したことをビルド出力で確認。
- 1.2 APIクライアント: `src/client/api/client.js` の `response.json()` を try/catch で包み、解析失敗時は日本語の固定メッセージのErrorを投げるよう修正。
- 1.3 SSE解析保護: `src/client/api/useServerState.js` に `parseSseData()` を追加し、`connected`/`update` リスナーの `JSON.parse` を保護。解析失敗時は `console.warn` して当該イベントを無視。
- 1.4 リクエストサイズ上限: `src/server/index.mjs` の `readJsonBody` に2MB上限を追加。超過時は413を返す（`statusCode` 付きErrorをthrowし、トップレベルの`catch`で処理する経路を新設）。3MBのボディを送って413が返ることを実測確認。
- 1.5 パス検査: `serveStatic` の `dist/` と `uploads/` の検査を、既存の `isPathInsideDirectory()`（`path.relative` 方式）に統一。正常なアップロード配信が引き続き200を返すことを確認。
- 1.6 メニュー表示中のCtrl+Z無効化: `src/client/pages/ScoreInputPage.jsx` に `menuOpen = editOpen || playerOpen || resetConfirmOpen` を追加し、メニュー表示中はキーボードショートカットのリスナー自体を外す。実ブラウザで、編集メニューを開いた状態でのCtrl+Zがundoを実行しないこと、メニューを閉じれば通常どおりundoできることを確認。

全項目: `npm test` 合格、`npx vite build` 成功、コンソールエラーなしを確認済み。

### 2026-07-03（3回目の点検）で動作を確認したもの

- コード分割後の全6ルート（Home / Viewer / Control List / Score Input / Settings / Preset Reorder）が正常に描画され、ページエラーなし。
- 2回目の一括修正6件にリグレッションなし（ビルド警告なし、単体テスト合格）。
- `app.json` のアトミック書き込み（一時ファイル→リネーム）が実装済みであることを確認（コーディングルール準拠）。
- 未コミットの新機能をAPI実測で確認:
  - ファウル: ストライク2以降は据え置き（3連打で strikes=2 のまま）、投球数は加算。
  - カウントRS: ボール・ストライクのみ0に戻す。
  - 四球（2塁走者のみの場面）: 打者が一塁へ、二塁走者は動かず（押し出しでないため）、得点変化なし。
  - 死球（満塁の場面）: 1点加算され満塁のまま。
  - 上記はすべて「戻る」で巻き戻せることを確認（点検後に状態を完全復元済み）。
  - 四球・死球は成績上 `others`（打数に含めない）として加算され、`data_model.md` の定義と整合。
- ただしスマホ幅の表示は 1.1 のとおり不具合あり（右側が切れて操作不能）。修正1行の有効性まで実証済み。
- 点検中に実装希望リストの作業がコミットされ（`1e62c0d`）、operation.md・button_list.md・rules.mdの実装リスト更新も同コミットに含まれていた。これにより「新機能のドキュメント未反映」の指摘は解消（1.1のスマホ表示の不具合は同コミット後も残存）。

### 2026-07-03（3回目の追加修正）で解決したもの

- スマホ幅でスコア入力画面の右側が切れる問題 → `gridTemplateColumns` のxs側を `minmax(0, 1fr)` に変更して修正済み。
- 自己ホストWebフォント未実装の問題 → D-DIN PRO / D-DIN PRO Condensed と Noto Sans JP のWOFF2を `src/client/fonts/` に同梱し、`@font-face` から読み込む構成へ修正済み。

### 2026-07-04（4回目の実画面テスト）で解決を確認したもの

- スコア入力画面のボタン折り返し: PC幅1366pxでは一行に入る分だけ横並び、入り切らないボタンは次行へ折り返し。スマホ幅390pxでは3列または2列へ折り返し、全ボタンが表示されることを確認。横スクロールなし、コンソールエラーなし。
- スコアボード上部の打者枠・投手枠: 境界線を上部バンドの中央へ寄せ、実画面で上下2段が等しい高さに見えることを確認。
- カウント表示: カウント文字を数px下げ、ランナー表示とアウト表示の間に収まる見た目へ調整。PC幅・スマホ幅ともに表示崩れなし。
- 英字略称フォント: SVG内にもD-DIN PRO Exp / D-DIN PROの `@font-face` を定義し、略称テキストへ明示適用。フォントファイルはローカルサーバーから200で配信されることを確認。

### 2026-07-04時点の実画面テストで残った問題

- 4回目の検証対象（ボタン折り返し、打者/投手枠、カウント位置、英字略称フォント）に限っては、実操作不能になる問題や画面外にはみ出す問題は見つからなかった。ただし対象をScore Input Page以外にも広げた5回目の点検で、1.3〜1.5の問題が新たに見つかった（詳細は `1. 直すべき問題` を参照）。

### 2026-07-04（5回目）で動作を確認したもの

- 打者枠・投手枠の縦幅は実測62px/62pxで完全に一致（SVG座標・実描画とも）。
- ランナー表示の下端とカウント表示の間隔、カウント表示の下端とアウト表示の間隔は、実測でそれぞれ-2px/-1pxとほぼ均等（1px差）。
- 編集メニューに「略称中央揃え」トグルが存在し、初期値OFFで仕様どおり。
- コード分割後の全6ルート（Home / Viewer / Control List / Score Input / Settings / Preset Reorder）を幅320〜1280pxで確認し、Score Input Page以外はボタンのはみ出し・横スクロールなし。単体テスト・ビルドとも成功、ページエラーなし。
- 検証で変更したチーム設定（略称拡大率の一時変更）は元の値（160%）に復元済み。
- Viewer Pageのツールバーを固定8列から自動折り返しグリッドへ変更
- ViewerPage 1箇所、SettingsPage 4箇所の flexWrap を sx={{ flexWrap: "wrap" }} に統一
- scoreboard_design.md から略称の自動縮小方針を外し、手動調整方針へ変更
